MIGRATION to THREEJS:



Classes to be changed:

SphereBufferGeometry to SphereGeometry, just a renaming




When using as module:
exports not accessible, in Annotations for icons
jquery must be removed but is highly attached to rendering and object mgmnt like annotationw shich are based on html and dom 










TODO GLTF produce errors  due to older js style. Needs to be UPDATED

Geometry was changed for BufferGeometry and its corresponding methods
SRGBEncoding for sRGCCololrspace
MAth was replaced by MathUtil.Math 
MOst functions now require a vector to store results instead of operating on the object.


Existing Errors:

viewer.html works fine

ElevationProfile.html Works, but the Spheres do not show color.  <-----

features_sorvilier.js Start but requires loaders

gradient_colors.js Errors had errors on html. Fixed. box.getCenter added  an empty vector to operate and compute results		

light_ca_13.html pointcloud.vs got a temporary fix, so only the one shadowmap is being applied. The 

In HTML creates an Animation PAth which nos gets a BufferGeometry, uses it to create THREE.LineBasicMAterial, then  THREE.Line


load_project.html 
LineSegmentsGeometry and others fixed, placed yet in /libs/thre.js/lines
ES5 was swhitched to ES6 and class inheritance.
It fixes now tools like point but the color of the sphere is still black, illumination is required

meshes.html does not work due to missing loaders

page.html works because is a catalog

toolbar.html Fixed with LineSegmentsGeometry and related classes migrated and VS FS

360 works fine

Annotations works fine

CameraAnimation works fine

cesiumSorvellier.html works fine

customSidebarSection.html works fine

embedded_iframe works ok

ept.html works fine

geopackage.html Works fine after fixing lineMAterial




light.html Works fine. Shadow works fine

lion_laz.html works fine

matcap.html works fine

multiple_pointclouds work fine

viewer.html works fine

AnimationPAths work fine 

Arena4D.html does not work.  PointCloudArena4DGeometrey is not defined

cesium_ca13.hrtml fine

classifications.html work fine

device_orientation.html workl fine


ept_binary.html fine


features_ca13.html Apparently works fine.  <----- Check it

light_animated.html Works fine. Shadowmaps fixed but only on single shadow. Uses PointcloudSM


lines.html Works fine. It is a single line and color applied

measurements.html Work fine but the sphere color does not show color.

orientedImages OrientedImage.js DONE  mesh.getWorldDirection now requires a target vector to copy values to.
Most methods should be checked for a target now. 



shapefiles.html Work Fine


VR tampoco funciona


Added COPC examples

ept.html works fine
copc.html  Works, required extra files
ept_laz14.html
ept_binary.html does not work. REquirec COPC. BinaryLoader does not work, requires node.url
ept_standard.html does not work. REquirec COPC. BinaryLoader does not work, requires node.url
